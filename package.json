{
  "name": "klb-chat-boot-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "dashboard"
  ],
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix dashboard",
    "build:backend": "npm run build --prefix backend",
    "build:dashboard": "npm run build --prefix dashboard",
    "build": "npm run build:backend && npm run build:dashboard",
    "start": "npm start --prefix backend"
  }
}
